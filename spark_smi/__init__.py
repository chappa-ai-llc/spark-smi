# Single source of truth for the package version. The footer (app.py) and
# pyproject.toml's [project] version are kept in sync with this literal by
# hand for now (dynamic versioning from this file is not required yet).
VERSION = "4.0.0a1"
__version__ = VERSION
